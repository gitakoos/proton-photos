<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security policy. This is NOT an egress allowlist: a Network Security Config governs
    cleartext traffic and trust anchors, not which hosts the app may reach over HTTPS. Egress is
    constrained instead by the app's own host usage and by certificate pinning on the Proton
    OkHttp clients (see CoreModule) for the API and the CDN/thumbnail traffic.

    base-config: cleartext disabled globally, and only the system trust store is trusted (no
    user-added CAs), which resists casual local MITM of API/CDN traffic.

    The per-domain entries restate the cleartext ban for the Proton API, account, alt-routing and
    CDN block hosts (all proton.me subdomains via includeSubdomains) and the two DoH resolvers
    (quad9.net, cloudflare-dns.com; no Google DoH). HTTPS to the other hosts the app legitimately
    uses (the GitHub update check, OpenStreetMap map tiles) still works via the system trust store.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">proton.me</domain>
    </domain-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">quad9.net</domain>
    </domain-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">cloudflare-dns.com</domain>
    </domain-config>
</network-security-config>
