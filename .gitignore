*.iml
.gradle
.kotlin
/local.properties
/.idea/
.DS_Store
/build
/app/build
/baseline-profile/build
/captures
.externalNativeBuild
.cxx
local.properties
*.jks
*.keystore
keystore.properties
*.har
*.log
/test_out*.txt
.release_notes_v*.md
# Internal developer docs: keep the docs/ folder (logo.png) tracked, but the
# markdown docs (and the archived release notes) stay local and off GitHub.
docs/*.md
docs/release-notes/

# Local reference clones of upstream repos (Ente, Immich, Drive), for cross-checking only.
temp*/

# Gradle wrapper except the jar
!gradle/wrapper/gradle-wrapper.jar
